<!--Nel partial si vede la variabile "line_item" perché questo è un automatismo di Rails, sostanzialmente é il nome del partial.
Ovvero: il nome del partial è "_line_item" e quindi il nome di variabile su cui cicla é "line_item"-->
<% if line_item == @current_item %>
    <tr id="current_item">
<% else %>
    <tr>
<% end %>
<td><%= line_item.quantity %> &times;</td>
<td><%= line_item.product.title %></td>
<td class="item_price"><%= number_to_currency(line_item.total_price) %></td>
</tr>

