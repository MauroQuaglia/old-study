* studiare la gemma bundler
* Capire bundle exec a cosa serve
* capire Rack
* capire WEBrick
* studiare get, puth, patch. delete, post.
** post method -> create() action
* capire meglio File.expand_path
* Nei javascript sotto assets mettere: use 'strict'
** capire meglio perché
* vedere come impostare il git sul singolo progetto e non su quello globale: git config --global. In realtà bisogna
studiare come impostare git.
* guardare come funziona byebug perché può essere utile per debuggare


------------------------------------------------------------------------------------------------------------------------
Quando ho creato l'applicazione, è stato fatto bundle e le ultime righe erano:

...
Using web-console 2.2.1
Bundle complete! 12 Gemfile dependencies, 53 gems now installed.
Use `bundle show [gemname]` to see where a bundled gem is installed.
         run  bundle exec spring binstub --all
* bin/rake: spring inserted
* bin/rails: spring inserted

D: Capire perché vuole [run  bundle exec spring binstub --all]. Io al momento non l'ho fatto.

------------------------------------------------------------------------------------------------------------------------

Capire come ridurre al minimo il Gemfile, intanto se lascio solo la gemma rails si arrabbia l'applicazione di default
perché cerca di usare SQLlite.
Comunque ci sono gemme, tipo 'coffee-rails' che attualmente non uso.

------------------------------------------------------------------------------------------------------------------------

dove sta il /.bundle che viene ignorato nel gitignore e a cosa serve?

------------------------------------------------------------------------------------------------------------------------

lanciare con rspec solo i test di una certa suite. Del tipo rake spec:models

------------------------------------------------------------------------------------------------------------------------