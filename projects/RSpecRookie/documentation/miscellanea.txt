ESEMPI DI CONFIGURAZIONE

Definisco un alias per un gruppo di test, così che invece di describe o context posso usare detail.
Definisco un comando detailed per la riga di comando per far girare solo i test che sono marcati con detail.

RSpec.configure do |c|
  c.alias_example_group_to :detail, :detailed => true
end

rspec nested_example_group_aliases_spec.rb --tag detailed -f d

------------------------------------------------------------------------------------------------------------------------


Uno dei modi per mettere in pending:
it "does something that is pending", :pending => true do
end

Uno dei modi per saltare un test:
it "does something that is skipped", :skip => true do
end

------------------------------------------------------------------------------------------------------------------------

Si possono fare anche cose carine del tipo:

RSpec.describe 9 do
  it { is_expected.to be > 8 }
  it { is_expected.to be < 10 }
end

------------------------------------------------------------------------------------------------------------------------