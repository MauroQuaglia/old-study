RSpec.describe "something _spec" do
  it "does something that passes _spec" do
    expect(5).to eq(5)
  end

  it "does something that fails _spec" do
    expect(5).to eq(4)
  end

  it "warnings _spec" do
    @var
  end

end