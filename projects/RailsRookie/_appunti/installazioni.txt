Installazione per avere Rails funzionante
* Ruby
* Ruby on Rails
* un interprete JavaScript (per linux è da installare)
* librerie varie che dipendono dal SO (quando servono si installano con apt-get)
* un database
* un editor
* un web server se voglio deployare in produzione insieme a un interprete per il codice

------------------------------------------------------------------------------------------------------------------------

Linux:
(potrebbero servirmi alcune librerie, ma eventualmente me le chiederà il SO di volta in volta)

installo RVM
* curl -sSL https://get.rvm.io | bash -s stable
* chiudere il terminale e riaprirlo per ricaricare il file .bash_login
* rvm requirements --autolibs=enable (installazione dei prerequisiti per il SO in uso)

installo Ruby con RVM
* rvm install 2.0.0
* rvm --default 2.0.0 (ogni shell ora usa l'interprete ruby 2.0.0, infatti potendone evere diversi non saprebbe quale ruby usare)

installo la gemma Rails via RubyGems che è distribuito con il Ruby che è stato precedentemente installato:
* gem install rails --version 4.0.0 --no-ri --no-rdoc
* rails -v (controllo)

------------------------------------------------------------------------------------------------------------------------

Per tenere sotto controllo le varie cose che sto usando:
Ruby   -> aggiungo il file .ruby-version con la versione usata nell'applicazione
gemset -> aggiungo il file .ruby-gemset con il nome del gemset usato
rails  -> nel gem file fisso la versione usata nell'applicazione.


RVM (“Ruby Version Manager”)
RVM ti permette di installare e gestire multiple installazioni di Ruby sul tuo sistema.
Ti permette anche di gestire gemsets differenti.
Per pulire i gemset, posso anche essere drastico, del tipo: rvm gemset delete nome-gemset. Poi lancio bundle dalla dir
di progetto e lui, dato che vede il file .ruby-gemset crea il gemset con quel nome e ci mette le gemme che ci sono specificate
nel gemfile con le relative dipendenze.


------------------------------------------------------------------------------------------------------------------------
rails è una gemma di ruby.
bundle è una gemma di ruby.

è come dire apro un progetto ruby vuoto, e mi faccio tutta l'architettura rails da zero.
oppure
scarico la gemma rails (è come una reference di c# piò o meno) e con rails new MyApp mi crea tutta l'infrastruttura nel progetto ruby.

Meglio avere gemset separati per ogni applicazione così le gemme non si mischiano. e con rvm posso cambiare la versione
di ruby da provare sopra al mio gemset.

come specificare quale ruby e gemset usare all'interno del progetto
http://stackoverflow.com/questions/15708916/use-rvmrc-or-ruby-version-file-to-set-a-project-gemset-with-rvm




------------------------------------------------------------------------------------------------------------------------


Installazione Linux:



Ma se si vuole una installazione separata di Ruby sulla propria macchina dedicata al supporto dell'applicazione bisogna usare RVM.
* curl -sSL https://get.rvm.io | bash -s stable
* chiudere il terminale e riaprirlo (ricarica il file .bash_login)
* rvm requirements --autolibs=enable (installazione dei prerequisiti per il SO in uso)
* rvm install 2.0.0 (installazione di ruby)
* rvm --default 2.0.0  (ogni shell ora usa l'interprete ruby 2.0.0, infatti avendone diversi non saprebbe quale ruby usare)
* gem install rails --version 4.0.0 --no-ri --no-rdoc (installazione rails)
* rails -v (controllo della versione installata)

----
dove è installato ruby:
which ruby

Posso anche creare tutta la documentazione in locale in modo da non dover cercare online.
rake doc:rails, poi la copi sul desktop ed è fatta.


Vedere le gemme di rails installate (notare che nei nomi compare sempre rails)
gem list --local rails

RVM serve per gestire più ambienti ruby contemporaneamente.
Sito ufficiale dove cercare informazioni: http://api.rubyonrails.org/

NB
http://freegoweb.it/stream/ruby-e-rails-in-italiano-parte-1

Database
Se si vuole usare un database diverso da SQLite3, bisogna installare un driver di database e una libreria che ci permette di
connetterci ed usare il database.
Si possono scaricare già compilati o da compilare, in tal caso serve un compilatore installato nel so.



Linux:
* sudo apt-get install ruby2.0.0
* sudo gem install rails --version 4.0.0 --no-ri --no-rdoc
